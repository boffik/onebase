# Прогресс по ветке `fix/platform-bugs`

Исходный список: `c:\Project\trade\замечания_по_платформе.md` (22 пункта).

## Партия 1 — баги и сразу очевидные фиксы

- [x] **#13** — в `*.proc.os` видна только одна процедура → `Interpreter.LookupSiblingProc` + `Registry.GetSiblingProc` (резолв helper-процедур по файлу).
- [x] **#17** — Ref-типы в измерениях регистров → починено коммитом `1ffa7bf` (`resolveRefArg` в `internal/storage/register.go`).
- [x] **#18** — виртуальное поле `Ссылка` в запросах → транслируется в `id` и с префиксом (`Н.Ссылка`), и без.
- [x] **#20** — `Справочники.X.ИмяПредопределённой` → возвращает `*Ref`. Добавлены `CatalogsRoot/CatalogProxy`.
- [x] **#21** — `НайтиПоНаименованию` / `НайтиПоКоду` → методы `CatalogProxy`, storage-метод `FindCatalogByField`.
- [x] **#22** — `Если/Иначе` теряет присваивание во внешнем scope → код уже корректен (`feae0205`). Добавлен регресс-тест.

## Партия 2 — малые фичи

- [x] **#3** — `ПустаяСсылка(x)` / `IsEmptyRef` — узкий предикат.
- [x] **#8** — `ЧислоПрописью(сумма, валюта)` со склонением рубль/рубля/рублей.
- [x] **#12** — параметры функций по умолчанию: `Функция X(А, Б = 20)`.
- [x] **#19а** — системные колонки регистра как `Период` / `ВидДвижения` / `Регистратор` / `НомерСтроки`.

## Партия 3 — средние фичи

- [x] **#4** — атрибуты регистров в outer SELECT/WHERE через `MIN(attr_col)` в inner-subquery.
- [x] **#5** — `Утилиты.Функция()` namespaced module call.
- [x] **#6** — `numerator.scope: Организация` — отдельные счётчики для каждой организации.
- [x] **#9** — `Распределить(сумма, веса[, точность])` — пропорциональное распределение с гарантией суммы.
- [x] **#10** — `.os` печатная форма перебивает YAML с тем же именем, в stderr warning.
- [x] **#11** — `demo.enabled=true` требует `ONEBASE_ENV=demo`, иначе отказ запускаться.
- [x] **#15** — события табличных частей `ПриДобавленииСтроки` и т.д. (FormInterpreter готов; UI-триггеры отдельно).
- [x] **#16** — `onebase run --watch` — hot reload `.os`/`.yaml` без рестарта.
- [x] **#19б** — функции даты в DSL: `Год` / `Месяц` / `НачалоДня` и т.д. → SQL для SQLite и PostgreSQL.

## Не входит в эту ветку (нужно обсуждение)

🔴 **Архитектурные** (недели работы каждая):
- **#1** МоментВремени / Граница() у виртуальных таблиц — меняется схема query + регистр + проведение.
- **#2** Управляемые блокировки `Блокировка.УстановитьЗначения(...)` — новый рантайм + SQL row locks.
- **#7** «Реквизит журнала» вместо `fallback` — рефакторинг journal.go и UI.
- **#14** Cross-ref в predefined (deferred FK / порядок вставки) — миграции схемы.

## Прогон

```powershell
$env:PATH = "$env:USERPROFILE\go-sdk\go\bin;$env:PATH"
go test ./...
```

На момент финала — всё зелёное по всему репо.
