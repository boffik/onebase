package equipment

// disconnect отключает устройство в тестах best-effort (план 109, класс 4).
// Ошибка отключения клиента от уже закрытого фейкового сервера не индикативна
// (зависит от гонки закрытия и даёт флаки-тесты), поэтому сознательно игнорируется.
func disconnect(d interface{ Disconnect() error }) { _ = d.Disconnect() }
