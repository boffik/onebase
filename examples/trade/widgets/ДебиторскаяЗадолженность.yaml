name: ДебиторскаяЗадолженность
type: kpi
title: Должники нам
format: number
query: |
  ВЫБРАТЬ CAST(СУММА(СуммаОстаток) AS INTEGER) КАК Значение
  ИЗ РегистрНакопления.Взаиморасчеты.Остатки()
  ГДЕ СуммаОстаток > 0
