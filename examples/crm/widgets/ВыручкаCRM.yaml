name: ВыручкаCRM
type: kpi
title: Выручка (выигранные сделки)
format: number
params:
  Начало: "{{today|-90d}}"
query: |
  ВЫБРАТЬ CAST(СУММА(Сумма) AS INTEGER) КАК Значение
  ИЗ Документ.Сделка
  ГДЕ Результат = 'Выиграна'
    И Дата >= &Начало
