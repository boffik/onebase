name: КонверсияВоронки
type: kpi
title: Конверсия (% выигранных)
format: percent
params:
  Начало: "{{today|-90d}}"
query: |
  ВЫБРАТЬ
    КОЛИЧЕСТВО(CASE WHEN Результат = 'Выиграна' THEN 1 END) * 100 / КОЛИЧЕСТВО(*) КАК Значение
  ИЗ Документ.Сделка
  ГДЕ Дата >= &Начало
