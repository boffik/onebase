name: СреднийРазмерСделки
type: kpi
title: Средний размер сделки
format: number
params:
  Начало: "{{today|-90d}}"
query: |
  ВЫБРАТЬ CAST(СУММА(Сумма) / КОЛИЧЕСТВО(*) AS INTEGER) КАК Значение
  ИЗ Документ.Сделка
  ГДЕ Результат = 'Выиграна'
    И Дата >= &Начало
