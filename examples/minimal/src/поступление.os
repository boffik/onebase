Процедура ПриЗаписи()
  Если this.Номер = "" Тогда
    Error("Номер обязателен");
  КонецЕсли;
  // Расчёт суммы строк табличной части
  Для Каждого Строка Из this.Товары Цикл
    Строка.Сумма = Строка.Количество * Строка.Цена;
  КонецЦикла;
КонецПроцедуры
